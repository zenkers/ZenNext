# ZenNext

> A Vue.js project which takes an array of JSON objects, and displays that information in a two-way navigation.

## Build Setup

``` bash
# install dependencies
npm install

# serve with hot reload at localhost:8080
npm run dev

# build for production with minification
npm run build
```